name: Pet Shop CI Pipeline 🚀

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
jobs:
  build-and-test:
    runs-on: ubuntu-latest 

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Build Backend Docker Image ☕
      run: |
        cd backend
        docker build -t petshop-backend .
        
    - name: Build Frontend Docker Image ⚛️
      run: |
        cd frontend
        docker build -t petshop-frontend .